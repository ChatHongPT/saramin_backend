# MongoDB 설정
MONGO_URI = "mongodb://localhost:27017/"

# PostgreSQL 설정
POSTGRES_CONFIG = {
    "dbname": "job_database",
    "user": "username",
    "password": "password",
    "host": "localhost",
    "port": 5432
}

# Redis 설정
REDIS_CONFIG = {
    "host": "localhost",
    "port": 6379,
    "db": 0
}
